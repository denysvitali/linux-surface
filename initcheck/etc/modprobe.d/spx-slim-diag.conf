# SPX audio is now loaded as a normal boot-time stack. Keep this file as the
# place for future diagnostic overrides, but do not blacklist the live path.
# The controller is loaded by spx-audio-modules.service after ADSP is running;
# suppress DT alias autoloading, which races remoteproc startup.
blacklist slim_qcom_ngd_ctrl
